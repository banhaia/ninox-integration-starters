# chatbot-stock

Ejemplo minimo para buscar productos por texto y responder con stock, precio y variantes.

## Ejecutar

Primero compilar el template:

```bash
cd templates/node-typescript
npm install
npm run build
```

Luego:

```bash
node ../../examples/chatbot-stock/run.js "remera negra"
```

