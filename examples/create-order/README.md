# create-order

Base para preparar el envio de pedidos a Ninox. El endpoint final y el schema exacto no estan confirmados en la documentacion consumible del repo, asi que este ejemplo deja el payload listo y llama al placeholder del cliente.

## Ejecutar

```bash
cd templates/node-typescript
npm run build
node ../../examples/create-order/run.js
```

